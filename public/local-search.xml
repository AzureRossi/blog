<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>视觉表征漫谈——从VAVAE到RAE</title>
    <link href="/blog/2026/03/07/%E8%A7%86%E8%A7%89%E8%A1%A8%E5%BE%81/"/>
    <url>/blog/2026/03/07/%E8%A7%86%E8%A7%89%E8%A1%A8%E5%BE%81/</url>
    
    <content type="html"><![CDATA[<h2 id="表征和VAE、Diffusion的关系"><a href="#表征和VAE、Diffusion的关系" class="headerlink" title="表征和VAE、Diffusion的关系"></a>表征和VAE、Diffusion的关系</h2><p>事情的一开始要从VAVAE和REPA说起。</p><p><img src="/blog/img/pipe.png" alt="VFM与VAE和Diffusion的关联"></p><p>视觉表征（Visual Foundation Model，以下统一简写成VFM），诸如DINOv2、DINOv3、MAE，让神经网络在高维的隐空间（Latent Space）中学会了理解现实世界的深层物理与语义逻辑，继而可以使用这个通用的隐空间表征作为各种复杂下游视觉任务的先验，完成五花八门的下游任务。</p><span id="more"></span><p>很长时间以来，VFM并没有对2D图像生成有着直接的冲击，更多的工作是将VFM以一种归纳偏置的形式引入生成pipeline，帮助和优化结果生成，诸如image editing等等任务。事情的转变要从两个2024-2025年的工作说起。</p><h3 id="VFM与VAE"><a href="#VFM与VAE" class="headerlink" title="VFM与VAE"></a>VFM与VAE</h3><p>潜在扩散模型（LDM）在生成高保真图像的方面在近年来表现非常出色。然而，在VAE的设计中始终存在一种困境：为了增加VAE重建原图的<strong>重建能力</strong>，最native的方案是增加VAE latent中channel的数量，比如将经典的SD-VAE的4 channel或者8 channel增加到32 channel，势必会增加VAE的重建性能。但是增加channel带来的代价是巨大的，一方面，VAVAE论文【1】指出当增加K倍的channel时，LDM达到对应的生成性能所需的数据量是<strong>指数级（2^K倍）</strong>的，这种指数级别的数据scale是很难接受的。另一方面，增加channel后想要达到相同的生成性能需要扩大DiT的模型规模，同时还要给予DiT更多的训练迭代时间。</p><p>所以，现在的LDM中VAE的训练和设计是一个trade-off：一方面latent的channel过小会导致latent信息损失较大，最终DiT生成产生视觉伪影；一方面过大的latent channel会使得计算成本过高从而无法收敛。</p><p>这个反直觉的现象其实有一个很自然的类比——在离散tokenizer（VQGAN&#x2F;MagViT）中，当codebook尺寸越来越大时，codebook利用率会急剧下降，出现”codebook collapse”的问题。连续VAE里高维latent的情形与之本质相同：<strong>当隐空间维度过大，模型学不会如何在这个空间里高效采样</strong>。</p><h3 id="两条路线的解法：REPA和VAVAE"><a href="#两条路线的解法：REPA和VAVAE" class="headerlink" title="两条路线的解法：REPA和VAVAE"></a>两条路线的解法：REPA和VAVAE</h3><p>解决这个问题的思路其实很直接——既然DiT在latent space里lost，是因为这个space毫无语义结构，那就给它一个语义锚点。于是社区出现了两条同期的平行路线：</p><p><strong>路线A（REPA，2410.06940）：在DiT侧打补丁</strong></p><p>REPA的思路是不动VAE，在DiT训练时额外加一个辅助loss，让DiT的中间hidden states与冻结的DINOv2特征对齐：</p><figure class="highlight pf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs pf">L_total = L_velocity + λ · L_REPA<br>L_REPA = -E[sim(DINOv2 patch feature, DiT hidden <span class="hljs-keyword">state</span> projection)]<br></code></pre></td></tr></table></figure><p>有一个值得注意的设计细节：对齐只发生在<strong>前几个</strong>Transformer block。实验发现前几层对齐了语义之后，后续层会自然地去专注高频细节的学习。这其实说明一件很有意思的事情：扩散模型从语义到细节的生成也有类似的层次分工，只是没有人显式地告诉它应该怎么分工——REPA相当于在早期layer强制建立了语义理解的”地基”，让模型自然而然地完成剩下的”装修”。结果：SiT在400K步就达到了原始SiT 7M步的效果，<strong>17.5倍</strong>训练加速。</p><p><strong>路线B（VAVAE + LightningDiT，2501.01423）：在VAE侧打补丁</strong></p><p>VAVAE这篇文章指出此困境来自学习无约束高维潜在空间内在的困难。用白话讲，就是DiT拟合VAE压缩的大channel latent太难了，因为VAE压缩完的latent本身不怎么包含语义，更多是一种高频信息和低频信息的压缩表示，从随机噪声完全学习这种数据流形自然time-costing &amp; expensive。最高效、最巧妙的方式，是让DiT在学习前就带有语义先验。</p><p>VAVAE从VAE训练时入手，在VAE训练过程中对齐潜在空间与预训练冻结的VFM特征，引入了两个loss：</p><ul><li><strong>mcos loss</strong>（边际余弦相似度）：逐patch强制VAE latent和DINOv2特征的绝对对齐</li><li><strong>mdms loss</strong>（边际距离矩阵相似度）：保证pair-wise相对结构的对齐</li></ul><p>两个loss都加了margin，用ReLU截断——只对”对齐不够好的部分”施加惩罚，避免过约束VAE的重建能力。结合自适应权重平衡重建和对齐的梯度幅度，整个VF Loss非常plug-and-play。</p><p>这样一来，diffusion model就不需要拟合毫无语义的像素层面的高频细节，而是专注于生成符合语义逻辑的structure，收敛速度自然就快很多。最终：LightningDiT在<strong>64 epoch</strong>就达到了FID 2.11（原始DiT需要1400+ epoch），<strong>21倍</strong>训练加速，最终FID 1.35 SOTA。</p><p>如果了解过3D生成的人可能会轻易get到这个点，在feed-forward 3D generation没有火之前，传统的SDS生成加速优化的最优方式是：用3D diffusion初始化3DGS。类比VAVAE，就是在训练DiT前，先在训练VAE过程中通过强制对齐VFM，把DiT的”起点”从随机噪声提升到具有语义结构的latent space——这和3DGS初始化的直觉完全一致。</p><hr><h2 id="VAVAE够好吗？——一些观察到的问题"><a href="#VAVAE够好吗？——一些观察到的问题" class="headerlink" title="VAVAE够好吗？——一些观察到的问题"></a>VAVAE够好吗？——一些观察到的问题</h2><p>Good but not enough，作为codebase和baseline, VAVAE看起来设计的确实简洁而巧妙。但当我真正动手复现和研究的时,你会发现他可以做到更多。</p><p><img src="/blog/pic/1391772792651_.pic_hd.jpg" alt="VAVAE的问题分析与VAE latent特征可视化"></p><p>上图右侧是VAE latent的特征可视化对比（Input | VAE latent | +DINOv2 | +MAE）。确实，加了VFM对齐之后，latent的”色彩”和”分布”看起来更有语义感了。但是Think deep:</p><p><strong>问题1：训练成本太高，且效果上限存疑</strong></p><p>VAVAE的训练需要在VAE训练过程中同时optimize重建loss和VF Loss。这个过程相当expensive——作者承认需要<strong>32卡×80G GPU</strong>训练3-5天才能出promising的结果。更关键的是，从根本上来说，你是在让一个以重建为目标训练的VAE encoder同时学习语义表征。这两个目标是存在内在张力的：重建要求保留所有细节（包括语义无关的高频纹理），语义表征需要主动过滤这些细节。VF Loss作为正则项，只能缓解这个矛盾，不能消除它。</p><p>最终学到的latent虽然有语义，但<strong>相对于原本的DINOv3&#x2F;DINOv2远远不足</strong>。</p><p><strong>问题2：方案在优雅性上有待增强</strong></p><p>从工程角度看，VF Loss这个设计需要仔细调margin超参数，adaptive weighting的trick也增加了实现复杂度。之前强制对齐vae latent和语义表征的方案略显暴力。</p><p><strong>问题3：重建水平略有下降（minor但存在）</strong></p><p>加了VF Loss之后，VAE的重建指标（PSNR、SSIM）相比纯重建VAE会有轻微下降——虽然不大，但说明语义对齐确实在”消耗”一部分重建容量。</p><hr><h2 id="My-way：DinoOffsetVAE"><a href="#My-way：DinoOffsetVAE" class="headerlink" title="My way：DinoOffsetVAE"></a>My way：DinoOffsetVAE</h2><p>带着观察到的这些问题，我直接尝试了一个更直接的思路：<strong>我们不用在VAE训练时强迫它对齐语义表征，而是将语义表征作为VAE latent的修正项</strong></p><p>于是有了<strong>DinoOffsetVAE</strong>这个方案。</p><p><img src="/blog/pic/1401772792659_.pic.jpg" alt="DinoOffsetVAE架构设计"></p><p>思路very native：充分利用已经训练好的VAE和DINOv2，不过将DINOv2当作原始VAE latent的<strong>语义修正项（offset）</strong>，在不破坏重建能力的前提下提高latent的语义含量。</p><p>具体来说：</p><ol><li>VAE Encoder正常提取VAE Latent（冻结，不动）</li><li>DINOv2提取图像的语义特征（冻结，不动）</li><li>一个轻量级的<strong>Mapper（可训练）</strong>将DINOv2特征映射到和VAE latent同空间的Dino Offset</li><li><code>Adjusted Latent = VAE Latent + Dino Offset</code></li><li>用nn.Linear将Dino Offset映射到Decoder feature维度，与VAE Decoder融合后重建图像</li></ol><p>Loss函数包含两部分：</p><ul><li><strong>Rec loss</strong>：保证Adjusted Latent送进VAE Decoder后重建质量不下降</li><li><strong>Cos Sim loss</strong>：让Adjusted Latent的语义与DINOv2特征保持对齐</li></ul><p>这个设计的好处在于：VAE本身的重建能力完全不受影响（VAE Encoder冻结），语义对齐完全由Mapper来承担，两者解耦得很干净。相比VAVAE从scratch训VAE的做法，这个方案<strong>轻量得多</strong>，可以在少得多的资源下快速验证。</p><h3 id="实验结果"><a href="#实验结果" class="headerlink" title="实验结果"></a>实验结果</h3><p><img src="/blog/pic/1411772792671_.pic.jpg" alt="重建效果对比（原图 | Ours | VAVAE）"></p><p>从上面的城市街道场景的重建可视化来看，我们的方案（Ours）在细节保留上相比VAVAE更接近原图，尤其是在地面纹理、车辆细节和建筑物边缘方面。</p><p>定量结果更加清晰：</p><p><img src="/blog/pic/1421772792679_.pic.jpg" alt="定量结果对比表"></p><table><thead><tr><th>方法</th><th>Real PSNR↑</th><th>Real SSIM↑</th><th>CG PSNR↑</th><th>CG SSIM↑</th></tr></thead><tbody><tr><td>VAVAE-epoch50</td><td>30.58</td><td>0.8818</td><td>31.62</td><td>0.9127</td></tr><tr><td>VAVAE-epoch125</td><td>31.41</td><td>0.8911</td><td>32.35</td><td>0.9205</td></tr><tr><td><strong>Ours-epoch45</strong></td><td><strong>32.20</strong></td><td><strong>0.9061</strong></td><td><strong>33.24</strong></td><td><strong>0.9325</strong></td></tr></tbody></table><p><strong>仅需45 epoch</strong>，我们的方案就超过了VAVAE训练125 epoch的结果，无论是真实图像还是CG图像的重建指标均如此。这也说明”解耦语义对齐和重建”这个思路的优越性——当两个目标不再相互竞争，各自都能收敛得更好、更快。</p><!-- 当然，方案也有失败的地方。![DinoOffsetVAE的Bad Cases](pic/1471772792792_.pic.jpg)某些特定类型的图像重建效果不够理想——比如有大量细密纹理的图案（电话机转盘）、强烈的光影对比（户外场景）、以及文字类图像（书籍封面）。这些bad case告诉我：Mapper学到的offset对于DINOv2本身也不擅长处理的场景，自然也无法给出好的语义修正。**VAE的重建上限依然被DINOv2本身的特征质量所制约**。 --><hr><h2 id="Deep-question：为什么不直接用VFM做tokenizer？"><a href="#Deep-question：为什么不直接用VFM做tokenizer？" class="headerlink" title="Deep question：为什么不直接用VFM做tokenizer？"></a>Deep question：为什么不直接用VFM做tokenizer？</h2><p>VAVAE也好，DinoOffsetVAE也好，本质上还是在VAE这个框架内修修补补。一个更根本的问题：</p><p><strong>既然VFM特征对DiT训练这么有帮助，为什么不直接把VFM作为tokenizer？</strong></p><p>抛弃VAE，直接用DINOv3的特征做diffusion的目标分布，不是语义含量更高，更直接吗？</p><p><img src="/blog/pic/1431772792687_.pic.jpg" alt="直接用VFM做tokenizer的架构设计（Stage 1: Latent Alignment）"></p><p>方案是：冻结Foundation Encoder（DINOv3），在其后接一个轻量级的trainable Adapter，将特征”适配”到目标latent维度，然后接一个trainable Decoder做重建。</p><p>结果是……</p><p><img src="/blog/pic/1441772792701_.pic.jpg" alt="直接用VFM特征重建的结果，从开始迭代到后来迭代的过程"></p><p>我们直接快进到收敛迭代的结果：</p><p><img src="/blog/pic/1451772792711_.pic.jpg" alt="DINOV2/DINOV3能重建主体语义，但缺少细节感知"></p><p><strong>DINOV2&#x2F;DINOV3能重建主体语义——物体的位置、大概形状、类别信息——但是严重缺少细节感知</strong>：</p><ul><li>路由器能认出是路由器，但细节纹理、天线的精细形状消失了</li><li>校车能认出是校车，但颜色不对、文字没了</li><li>香蕉的形状大概对，但颜色信息很差、叶子细节全无</li></ul><p>这说明VFM本身作为tokenizer是not enough的：DINO是在语义对比损失下训练的，它<strong>主动丢弃</strong>了高频、低语义的细节信息（颜色、纹理、精细纹路），因为这些信息对分类&#x2F;检测等判别任务反而是噪声。Representation learning的本质就是eliminating unnecessary details（LeCun的一句老话，但在这里找到了生成领域的具体体现）。</p><p>所以，直接用VFM做tokenizer的困境是：<strong>语义太多，细节太少</strong>——这恰好和VAE tokenizer的问题相反（VAE：细节太多，语义太少）。</p><p>这里还有一个不那么明显但很关键的工程问题。RAE论文（2510.11690）给出了一个严格的理论证明：当DiT的宽度（hidden dimension）小于token的channel维度时，flow matching loss存在一个<strong>与数据分布无关的正下界</strong>：</p><p>$$\mathcal{L}(g, \theta) \geq \sum_{i&#x3D;d+1}^{n} \lambda_i &gt; 0$$</p><p>从直觉上理解，扩散模型训练时会向数据注入高斯噪声，这相当于将数据流形”扩散”到整个高维空间——原本低维的流形支撑被扩展到了full-rank空间。如果DiT宽度不够，它根本没有能力表示这个full-rank分布，无论怎么训都无法收敛。</p><p>同期相同想法的RAE做了一个很有意思的实验验证：对一张图片过拟合，然后把DiT宽度从384逐渐增加到768。当宽度 &lt; token维度（768）时，彻底失败；当宽度 ≥ token维度时，立刻成功——<strong>是宽度，不是深度</strong>。</p><p>这说明直接用DINOv2-ViT-B&#x2F;16的768维特征做latent，需要相应地将DiT宽度也扩展到768以上。这不是没法做，但需要仔细的架构设计。</p><hr><h2 id="同期的撞车idea：SVG与RAE"><a href="#同期的撞车idea：SVG与RAE" class="headerlink" title="同期的撞车idea：SVG与RAE"></a>同期的撞车idea：SVG与RAE</h2><p>在纠结这两个问题（细节缺失 &amp; DiT宽度限制）的时候，同期出现了两个和我想法高度吻合的工作，分别独立地给出了自己的解答。</p><h3 id="SVG：清华自动化的同期方案"><a href="#SVG：清华自动化的同期方案" class="headerlink" title="SVG：清华自动化的同期方案"></a>SVG：清华自动化的同期方案</h3><p><img src="/blog/pic/1461772792777_.pic.jpg" alt="SVG架构：DINO + Residual Encoder（同期工作）"></p><p>SVG（LDM without Variational Autoencoder，2510.15301）是来自清华自动化的工作，<strong>完全抛弃VAE</strong>，提出了一个我看到就觉得一模一样的方案😥——用DINOv3特征加上一个<strong>Residual Encoder</strong>来构建新的latent space：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm">SVG Encoder = 冻结的<span class="hljs-keyword">DINOv3 </span>⊕ 轻量级Residual Encoder（可训练）<br>SVG Feature = <span class="hljs-keyword">DINO特征 </span>concat 残差细节特征<br>（<span class="hljs-keyword">Distribution </span>Alignment确保残差不破坏<span class="hljs-keyword">DINO的语义分布）</span><br></code></pre></td></tr></table></figure><p>Residual Encoder专门负责补充DINO特征里缺失的颜色和细粒度感知细节，两路特征在channel维度上直接concat。为了防止Residual Encoder的输出破坏DINO原有的语义分布，SVG对残差特征做batch-statistics级别的对齐：</p><p>$$\hat{F}_R &#x3D; \frac{F_R - \mu(F_R)}{\sigma(F_R)} \cdot \sigma(F_D) + \mu(F_D)$$</p><p><img src="/blog/new_pic/1.png" alt="SVG tokenizer的重建效果（好坏案例均有展示）"></p><p>其实这步是这个设计中我认为最关键的一步，既保留了DINO的强语义判别性，又补充了生成所需的感知细节。最终的SVG Feature兼具语义判别性和重建能力，是一个<strong>task-general</strong>的统一特征空间——同一套特征可以同时支持生成、感知和语义理解任务。</p><p><strong>为什么SVG能work？</strong> 从latent space的语义分散性（semantic discriminability）角度来分析：当latent space里同类别的样本聚集、不同类别的样本分散时，diffusion model的mean velocity方向在同类别内是一致的、在不同类别间是相互区分的。这种结构化的速度场使得：</p><ol><li>DiT学习更容易（方向更明确，歧义性低）</li><li><strong>天然支持few-step采样</strong>（ODE路径更直，5步就能得到好结果）</li></ol><p>对比之下，原始SD-VAE latent的语义纠缠使得不同类别的velocity方向混杂，DiT需要更多步骤来”解开”这种歧义。</p><p>SVG在ImageNet 256×256上（80 epoch，25步采样）: gFID&#x3D;6.57（w&#x2F;o CFG），gFID&#x3D;3.54（w&#x2F; CFG），训练500 epoch后进一步到gFID&#x3D;2.10（w&#x2F; CFG）——而且同时支持多个视觉任务，不只是生成。</p><h3 id="RAE"><a href="#RAE" class="headerlink" title="RAE"></a>RAE</h3><p>RAE（Diffusion Transformers with Representation Autoencoders，2510.11690）来自纽约大学，做的事情更激进：直接用<strong>冻结的VFM encoder</strong>（DINO&#x2F;SigLIP&#x2F;MAE）配上一个训练好的<strong>轻量ViT decoder</strong>，组成Representation Autoencoder（RAE），替换SD-VAE。</p><p>这个工作挑战了两个长期被视为”常识”的假设：</p><ol><li><p><del>“VFM特征不能用于重建，因为丢失了低级细节”</del><br>→ 实验证明：配上合适的ViT decoder，冻结的DINO&#x2F;SigLIP&#x2F;MAE特征重建质量<strong>全面优于SD-VAE</strong>（MAE-B的rFID 0.16，DINO-B的rFID 0.49，vs SD-VAE的0.62）</p></li><li><p><del>“高维latent space对DiT训练很困难”</del><br>→ 理论+实验证明：只要DiT宽度≥token维度，高维不是问题，反而是优势</p></li></ol><p><img src="/blog/new_pic/2.png" alt="RAE tokenizer的重建效果（包含典型失败案例）"></p><p>RAE系统性地解决了直接使用高维VFM特征的三个工程挑战：</p><ul><li><strong>宽度缩放</strong>：DiT宽度需≥768 → 引入DDT head（宽但浅），在不增加quadratic attention成本的前提下增加模型宽度</li><li><strong>噪声调度</strong>：原有基于分辨率的noise schedule shift没有考虑channel维度 → dimension-aware noise schedule</li><li><strong>解码器鲁棒性</strong>：RAE decoder训练时只见过干净latent，推理时DiT输出有小量误差 → noise-augmented decoder training</li></ul><p>最终，<strong>无需任何REPA-style的alignment loss</strong>（因为latent本身就是语义丰富的），FID 1.51（无guidance）&#x2F; <strong>1.13</strong>（有guidance），同时encoder计算量比SD-VAE少6×，decoder少3×。</p><p><img src="/blog/new_pic/3.png" alt="RAE DiT的生成质量——多类别（无CFG）"></p><p><img src="/blog/new_pic/4.png" alt="RAE DiT的类条件生成质量——北极狐（有CFG）"></p><p>两条路殊途同归，都指向同一个结论：<strong>VFM特征才是更自然的生成空间载体</strong>，只需要解决细节重建的问题，生成本身反而变得更容易。</p><!-- 从信息论的角度看，这个演进路径有某种必然性。VAE的优化目标是重建（保留所有信息），VFM的优化目标是语义理解（丢弃irrelevant details）。这两个目标**本质上是不兼容的**，仅靠alignment loss无法弥合这个根本张力。唯一的出路是直接用以语义理解为目标训练的特征空间作为生成空间——这正是RAE和SVG的做法。 --><hr><h2 id="还剩下什么问题？"><a href="#还剩下什么问题？" class="headerlink" title="还剩下什么问题？"></a>还剩下什么问题？</h2><p><img src="/blog/new_pic/5.png" alt="此类工作的发展脉络总结（阶段1→4及开放问题）"></p><p>回头看整条发展脉络，其实每一个阶段都是在前一个阶段的问题上打补丁，是重建和生成性能的trade-off, 并没有让二者互补，相得益彰。</p><p><strong>阶段1（VAVAE、REPA）</strong>：思路是在训练VAE的过程中强制语义正则化，让latent向DINOv2对齐。逻辑上没问题，但代价是训练时间极长——从一个完全不懂语义的latent硬学到足够的语义含量，32卡80G跑3-5天是常态。更根本的问题是效果上限：两个目标的内在张力始终存在，正则化力度有限，学出来的语义含量和DINO本身差得还很远。</p><p><strong>阶段2（SVG、RAE）</strong>：承认”冻结的DINO就是最好的语义来源”，直接把它当tokenizer用。SVG用残差分支补细节，RAE配轻量ViT decoder直接重建——训练成本直接砍掉一个数量级。但问题没有消失，只是换了个形式：SVG把残差维度控制在8维，是因为维度一大语义就稀释了，这个8维本身就是个妥协，不是什么精心设计。语义和重建之间的trade-off还在那里，只是被压缩进了残差的维度选择里。</p><p><strong>阶段3（Aligntok、UniLIP、Uniflow）</strong>：这批工作更激进——既然冻结DINO的语义上限突破不了，就干脆把DINO打开微调，在重建训练过程中用自蒸馏持续做语义监督。效果是有的，但开销和风险都很大：打开DINO之后训练里会涌现出各种trick，成本变得难以控制；更重要的是，用领域内分布去finetune一个通用VFM，泛化性的损失几乎是必然的，代价很难评估。</p><p>我自己在这条路上也摸索过一个不同的方向：<strong>保持DINO冻结，但让额外的重建分支在几乎不稀释latent语义的情况下大幅提升重建能力</strong>。具体探索了两个方案：</p><ul><li><strong>方案a</strong>：将VAE以极小的扰动藏入DINO的分布里——DINO主分支看不出扰动，但用于重建的decoder专门放大、捕获这个扰动来恢复细节。本质上是把额外信息”隐写”进DINO分布的边角空间。</li><li><strong>方案b</strong>：类似SVG的架构，保持小维度残差，但在残差接入decoder之前用一个投影矩阵把它放大到对重建更重要的表示空间——用参数而非维度来解决残差信息量不足的问题。</li></ul><p><strong>阶段4（iREPA、Semantic lead the way）</strong>：这是目前还在起步阶段的方向——不再纠结于tokenizer设计，而是让语义表征更上游地引导整个生成过程。</p><hr><p><strong>参考文献</strong></p><p>【1】Jingfeng Yao et al. “Reconstruction vs. Generation: Taming Optimization Dilemma in Latent Diffusion Models.” CVPR 2025. arXiv: 2501.01423</p><p>【2】Sihyun Yu et al. “Representation Alignment for Generation: Training Diffusion Transformers Is Easier Than You Think.” ICLR 2025. arXiv: 2410.06940</p><p>【3】Minglei Shi et al. “Latent Diffusion Model without Variational Autoencoder.” ICLR 2026. arXiv: 2510.15301</p><p>【4】Boyang Zheng et al. “Diffusion Transformers with Representation Autoencoders.” ICLR 2026. arXiv: 2510.11690</p>]]></content>
    
    
    <categories>
      
      <category>科研随笔</category>
      
    </categories>
    
    
    <tags>
      
      <tag>生成模型</tag>
      
      <tag>视觉表征</tag>
      
      <tag>Diffusion</tag>
      
      <tag>VAE</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/blog/2026/03/03/hello-world/"/>
    <url>/blog/2026/03/03/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
